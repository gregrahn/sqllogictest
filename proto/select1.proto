statement ok
CREATE TABLE t1(a INTEGER, b INTEGER, c INTEGER, d INTEGER, e INTEGER)

statement ok
INSERT INTO t1(e,c,b,d,a) VALUES(NULL,102,NULL,101,104)

statement ok
INSERT INTO t1(a,c,d,e,b) VALUES(107,106,108,109,105)

statement ok
INSERT INTO t1(e,d,b,a,c) VALUES(110,114,112,NULL,113)

statement ok
INSERT INTO t1(d,c,e,a,b) VALUES(116,119,117,115,NULL)

statement ok
INSERT INTO t1(c,d,b,e,a) VALUES(123,122,124,NULL,121)

statement ok
INSERT INTO t1(a,d,b,e,c) VALUES(127,128,129,126,125)

statement ok
INSERT INTO t1(e,c,a,d,b) VALUES(132,134,131,133,130)

statement ok
INSERT INTO t1(a,d,b,e,c) VALUES(138,136,139,135,137)

statement ok
INSERT INTO t1(e,c,d,a,b) VALUES(144,141,140,142,143)

statement ok
INSERT INTO t1(b,a,e,d,c) VALUES(145,149,146,NULL,147)

statement ok
INSERT INTO t1(b,c,a,d,e) VALUES(151,150,153,NULL,NULL)

statement ok
INSERT INTO t1(c,e,a,d,b) VALUES(155,157,159,NULL,158)

statement ok
INSERT INTO t1(c,b,a,d,e) VALUES(161,160,163,164,162)

statement ok
INSERT INTO t1(b,d,a,e,c) VALUES(167,NULL,168,165,166)

statement ok
INSERT INTO t1(d,b,c,e,a) VALUES(171,170,172,173,174)

statement ok
INSERT INTO t1(e,c,a,d,b) VALUES(177,176,179,NULL,175)

statement ok
INSERT INTO t1(b,e,a,d,c) VALUES(181,180,182,183,184)

statement ok
INSERT INTO t1(c,a,b,e,d) VALUES(187,188,186,189,185)

statement ok
INSERT INTO t1(d,b,c,e,a) VALUES(190,194,193,192,191)

statement ok
INSERT INTO t1(a,e,b,d,c) VALUES(199,197,198,196,195)

statement ok
INSERT INTO t1(b,c,d,a,e) VALUES(NULL,202,203,201,204)

statement ok
INSERT INTO t1(c,e,a,b,d) VALUES(208,NULL,NULL,206,207)

statement ok
INSERT INTO t1(c,e,a,d,b) VALUES(214,210,213,212,211)

statement ok
INSERT INTO t1(b,c,a,d,e) VALUES(218,215,216,217,219)

statement ok
INSERT INTO t1(b,e,d,a,c) VALUES(223,221,222,220,224)

statement ok
INSERT INTO t1(d,e,b,a,c) VALUES(226,227,228,229,225)

statement ok
INSERT INTO t1(a,c,b,e,d) VALUES(234,231,232,230,233)

statement ok
INSERT INTO t1(e,b,a,c,d) VALUES(237,236,239,NULL,238)

statement ok
INSERT INTO t1(e,c,b,a,d) VALUES(NULL,244,240,243,NULL)

statement ok
INSERT INTO t1(e,d,c,b,a) VALUES(246,248,247,249,245)

query I nosort
SELECT (SELECT max(a) FROM t1)
  FROM t1
 ORDER BY 1
 LIMIT 4

query I nosort
SELECT c-e
  FROM t1
 WHERE a>b
   AND c BETWEEN b AND d
   AND d>e
 ORDER BY 1
 LIMIT 4

query IIIIIIIIIIIII nosort
SELECT a-d,
       coalesce(a,b,c,d,e),
       d-e,
       b-d,
       a-e,
       a-b,
       c-e,
       (SELECT max(a) FROM t1),
       a-c,
       c,
       c-d,
       b-c,
       abs(a)
  FROM t1
 WHERE e+d BETWEEN a+b+10 AND c+130
 ORDER BY 4,6,10,12,2,1,8,7,3,13,11,9,5
 LIMIT 4

query IIIIIIIIIIIIIII nosort
SELECT b,
       coalesce(a,b,c,d,e),
       c-e,
       e,
       (SELECT max(c)-max(d) FROM t1),
       c-d,
       a-d,
       (SELECT min(b) FROM t1),
       a,
       a-c,
       d,
       abs(a+b),
       b-d,
       b-e,
       (SELECT max(a) FROM t1)
  FROM t1
 WHERE d>e
   AND a>b
 ORDER BY 8,10,9,3,14,1,12,2,6,4,7,5,11,15,13
 LIMIT 4

query IIIIIIII nosort
SELECT b-e,
       c,
       d,
       c-e,
       b-c,
       d-e,
       b,
       abs(a)
  FROM t1
 WHERE c BETWEEN b AND d
   AND a>b
   AND d BETWEEN 110 AND 120
   AND b>c
   AND c>d
 ORDER BY 7,5,2,4,3,1,8,6
 LIMIT 4

query IIIIIIII nosort
SELECT e,
       c-e,
       b-d,
       (SELECT max(c)-max(d) FROM t1),
       d-e,
       a-c,
       b-c,
       (SELECT max(a) FROM t1)
  FROM t1
 WHERE c BETWEEN b AND d
   AND a>b
 ORDER BY 3,5,4,7,8,2,1,6
 LIMIT 4

query IIIIIIIIIII nosort
SELECT (SELECT min(b) FROM t1),
       abs(a+b),
       b-c,
       coalesce(a,b,c,d,e),
       a-d,
       (SELECT max(c)-max(d) FROM t1),
       c-d,
       c,
       (SELECT max(a) FROM t1),
       a-b,
       e
  FROM t1
 WHERE c>d
    OR d BETWEEN 110 AND 120
    OR b>c
    OR a>b
    OR c BETWEEN b AND d
 ORDER BY 7,3,11,8,10,1,6,4,2,5,9
 LIMIT 4

query IIIIIIIIIII nosort
SELECT e,
       abs(a+b),
       a-e,
       d,
       (SELECT min(b) FROM t1),
       b,
       b-e,
       (SELECT max(a) FROM t1),
       abs(a),
       c-d,
       a-d
  FROM t1
 WHERE d BETWEEN 110 AND 120
   AND c>d
 ORDER BY 10,4,7,1,11,3,9,5,6,8,2
 LIMIT 4

query IIIIIIIIIII nosort
SELECT d-e,
       a-d,
       b,
       (SELECT max(a) FROM t1),
       (SELECT max(c)-max(d) FROM t1),
       coalesce(a,b,c,d,e),
       c-d,
       (SELECT min(b) FROM t1),
       a-c,
       a,
       abs(a+b)
  FROM t1
 WHERE c BETWEEN b AND d
    OR d>e
    OR e+d BETWEEN a+b+10 AND c+130
 ORDER BY 7,6,11,5,2,10,4,3,1,9,8
 LIMIT 4

query IIIIIIIIIIIIIIIIIII nosort
SELECT c-d,
       b-c,
       a-e,
       (SELECT max(c)-max(d) FROM t1),
       c-e,
       c,
       d,
       (SELECT max(a) FROM t1),
       b-d,
       abs(a+b),
       d-e,
       b,
       (SELECT min(b) FROM t1),
       b-e,
       a,
       a-c,
       coalesce(a,b,c,d,e),
       e,
       abs(a)
  FROM t1
 WHERE b>c
    OR d>e
 ORDER BY 19,3,7,8,6,9,1,4,12,17,2,5,14,15,16,18,10,11,13
 LIMIT 4

query IIIIIIIIIIIIIIII nosort
SELECT a-d,
       c,
       a-b,
       abs(a+b),
       (SELECT min(b) FROM t1),
       a-e,
       (SELECT max(c)-max(d) FROM t1),
       abs(a),
       d,
       e,
       b-c,
       b,
       a-c,
       b-d,
       a,
       c-e
  FROM t1
 ORDER BY 7,1,15,6,2,14,9,16,5,11,4,10,13,8,3,12
 LIMIT 4

query IIII nosort
SELECT b-c,
       b-e,
       a,
       (SELECT min(b) FROM t1)
  FROM t1
 WHERE c>d
    OR c BETWEEN b AND d
    OR a>b
 ORDER BY 3,4,2,1
 LIMIT 4

query IIIIIIII nosort
SELECT (SELECT max(a) FROM t1),
       a,
       b-c,
       e,
       a-d,
       d,
       c-e,
       abs(a+b)
  FROM t1
 WHERE c BETWEEN b AND d
 ORDER BY 3,2,8,1,7,4,5,6
 LIMIT 4

query IIIIIIIIIIIIIIII nosort
SELECT b,
       d-e,
       b-c,
       c-e,
       abs(a),
       (SELECT min(b) FROM t1),
       abs(a+b),
       a-e,
       d,
       (SELECT max(a) FROM t1),
       coalesce(a,b,c,d,e),
       b-d,
       a-c,
       a,
       a-b,
       e
  FROM t1
 WHERE e+d BETWEEN a+b+10 AND c+130
   AND d BETWEEN 110 AND 120
   AND b>c
   AND c BETWEEN b AND d
   AND d>e
   AND a>b
 ORDER BY 12,13,16,6,14,3,15,10,2,7,11,8,9,1,5,4
 LIMIT 4

query IIIIIIII nosort
SELECT c-d,
       (SELECT max(c)-max(d) FROM t1),
       e,
       abs(a),
       b-d,
       d-e,
       (SELECT max(a) FROM t1),
       a-c
  FROM t1
 WHERE e+d BETWEEN a+b+10 AND c+130
    OR b>c
    OR a>b
    OR c>d
    OR c BETWEEN b AND d
    OR d>e
 ORDER BY 5,4,8,1,2,3,7,6
 LIMIT 4

query IIIIIIII nosort
SELECT coalesce(a,b,c,d,e),
       a-e,
       c-d,
       a-c,
       b-c,
       a,
       (SELECT max(c)-max(d) FROM t1),
       abs(a+b)
  FROM t1
 WHERE d BETWEEN 110 AND 120
   AND e+d BETWEEN a+b+10 AND c+130
   AND c>d
   AND d>e
   AND b>c
 ORDER BY 1,8,5,3,2,6,7,4
 LIMIT 4

query IIIIIIIIIIIIIII nosort
SELECT b-d,
       c-d,
       e,
       a-b,
       (SELECT max(a) FROM t1),
       b-e,
       a-d,
       a-e,
       a-c,
       d-e,
       b,
       c,
       (SELECT min(b) FROM t1),
       coalesce(a,b,c,d,e),
       abs(a)
  FROM t1
 WHERE c>d
   AND c BETWEEN b AND d
   AND d BETWEEN 110 AND 120
 ORDER BY 14,3,13,4,2,12,8,7,5,10,9,6,1,15,11
 LIMIT 4

query IIIIIIIII nosort
SELECT d-e,
       (SELECT max(c)-max(d) FROM t1),
       abs(a+b),
       b-e,
       b,
       abs(a),
       e,
       (SELECT min(b) FROM t1),
       coalesce(a,b,c,d,e)
  FROM t1
 WHERE b>c
    OR c BETWEEN b AND d
    OR c>d
 ORDER BY 9,4,3,6,5,8,1,2,7
 LIMIT 4

query IIIIIIIIIIIIIIIIIII nosort
SELECT c-e,
       a-d,
       coalesce(a,b,c,d,e),
       e,
       b,
       a-b,
       b-c,
       (SELECT min(b) FROM t1),
       b-e,
       (SELECT max(c)-max(d) FROM t1),
       d,
       c-d,
       c,
       a-c,
       abs(a+b),
       (SELECT max(a) FROM t1),
       b-d,
       d-e,
       a-e
  FROM t1
 WHERE b>c
    OR a>b
    OR d>e
    OR d BETWEEN 110 AND 120
    OR c>d
    OR c BETWEEN b AND d
 ORDER BY 19,1,13,9,2,12,6,3,5,16,10,11,17,8,7,4,14,18,15
 LIMIT 4

query IIII nosort
SELECT b,
       d,
       a-d,
       abs(a+b)
  FROM t1
 WHERE e+d BETWEEN a+b+10 AND c+130
   AND a>b
 ORDER BY 2,4,1,3
 LIMIT 4

query IIIIIIIIIII nosort
SELECT d,
       b-e,
       a-b,
       a-c,
       a-d,
       b-c,
       a-e,
       e,
       d-e,
       b-d,
       abs(a+b)
  FROM t1
 WHERE a>b
   AND b>c
 ORDER BY 6,10,2,8,11,7,4,3,5,1,9
 LIMIT 4

query IIIIIIIIIIII nosort
SELECT (SELECT min(b) FROM t1),
       a-b,
       c-d,
       abs(a),
       c,
       abs(a+b),
       a-d,
       a,
       (SELECT max(c)-max(d) FROM t1),
       coalesce(a,b,c,d,e),
       b-c,
       a-c
  FROM t1
 WHERE e+d BETWEEN a+b+10 AND c+130
   AND c>d
   AND b>c
 ORDER BY 6,9,4,3,2,8,7,12,11,10,1,5
 LIMIT 4

query IIII nosort
SELECT a-c,
       c-e,
       c,
       abs(a)
  FROM t1
 WHERE d>e
    OR b>c
    OR e+d BETWEEN a+b+10 AND c+130
    OR a>b
 ORDER BY 1,2,3,4
 LIMIT 4

query I nosort
SELECT (SELECT max(c)-max(d) FROM t1)
  FROM t1
 ORDER BY 1
 LIMIT 4

query IIIIIIIIIIII nosort
SELECT b-c,
       b-d,
       (SELECT max(a) FROM t1),
       c,
       (SELECT min(b) FROM t1),
       a,
       a-b,
       c-d,
       coalesce(a,b,c,d,e),
       abs(a),
       d-e,
       c-e
  FROM t1
 WHERE d>e
   AND b>c
   AND a>b
   AND c BETWEEN b AND d
   AND e+d BETWEEN a+b+10 AND c+130
 ORDER BY 3,7,5,8,11,6,1,10,2,9,4,12
 LIMIT 4

query IIIIII nosort
SELECT b-e,
       (SELECT max(a) FROM t1),
       d-e,
       a-e,
       c,
       coalesce(a,b,c,d,e)
  FROM t1
 WHERE d BETWEEN 110 AND 120
   AND d>e
   AND c>d
   AND e+d BETWEEN a+b+10 AND c+130
 ORDER BY 4,3,1,2,5,6
 LIMIT 4

query IIIIIII nosort
SELECT a,
       c-e,
       b,
       b-e,
       b-d,
       (SELECT max(c)-max(d) FROM t1),
       a-c
  FROM t1
 WHERE c>d
   AND d BETWEEN 110 AND 120
   AND e+d BETWEEN a+b+10 AND c+130
   AND c BETWEEN b AND d
   AND d>e
   AND b>c
 ORDER BY 5,4,3,7,6,2,1
 LIMIT 4

query IIIIIIIIIIII nosort
SELECT b-c,
       a-b,
       b,
       a-d,
       coalesce(a,b,c,d,e),
       a-c,
       d-e,
       d,
       c-e,
       a-e,
       (SELECT min(b) FROM t1),
       c
  FROM t1
 WHERE c>d
    OR a>b
    OR b>c
    OR e+d BETWEEN a+b+10 AND c+130
    OR c BETWEEN b AND d
    OR d BETWEEN 110 AND 120
 ORDER BY 12,10,4,1,11,8,3,7,5,6,2,9
 LIMIT 4

query IIIIIIIIII nosort
SELECT d-e,
       abs(a+b),
       c-e,
       d,
       a-b,
       b-c,
       b,
       e,
       coalesce(a,b,c,d,e),
       a
  FROM t1
 WHERE c>d
 ORDER BY 6,3,1,2,7,9,4,5,10,8
 LIMIT 4

query IIIIIIII nosort
SELECT a-c,
       b-c,
       (SELECT max(a) FROM t1),
       abs(a+b),
       c-e,
       a,
       (SELECT min(b) FROM t1),
       b-e
  FROM t1
 WHERE c>d
    OR e+d BETWEEN a+b+10 AND c+130
    OR d>e
 ORDER BY 1,2,4,3,7,5,8,6
 LIMIT 4

query IIIIIIIIII nosort
SELECT a,
       coalesce(a,b,c,d,e),
       b-c,
       b-d,
       c-d,
       b-e,
       d-e,
       (SELECT max(a) FROM t1),
       a-d,
       (SELECT min(b) FROM t1)
  FROM t1
 WHERE e+d BETWEEN a+b+10 AND c+130
   AND d>e
 ORDER BY 9,4,1,7,3,2,10,5,8,6
 LIMIT 4

query IIIIIIIIIIIII nosort
SELECT d,
       (SELECT max(a) FROM t1),
       b,
       c,
       e,
       d-e,
       b-c,
       a-c,
       a,
       a-e,
       (SELECT min(b) FROM t1),
       c-d,
       a-d
  FROM t1
 WHERE d BETWEEN 110 AND 120
 ORDER BY 5,6,13,1,2,7,9,11,3,8,10,12,4
 LIMIT 4

query IIIII nosort
SELECT d,
       a-e,
       (SELECT max(c)-max(d) FROM t1),
       b,
       a-c
  FROM t1
 WHERE e+d BETWEEN a+b+10 AND c+130
 ORDER BY 5,4,2,3,1
 LIMIT 4

query IIIIIIIIIIIIIII nosort
SELECT (SELECT max(a) FROM t1),
       d-e,
       c-d,
       a-b,
       b-c,
       c,
       abs(a),
       coalesce(a,b,c,d,e),
       a-c,
       a,
       a-d,
       a-e,
       d,
       b,
       (SELECT min(b) FROM t1)
  FROM t1
 WHERE d BETWEEN 110 AND 120
 ORDER BY 12,1,4,8,15,5,14,13,6,10,3,7,11,2,9
 LIMIT 4

query II nosort
SELECT d,
       abs(a)
  FROM t1
 WHERE c BETWEEN b AND d
   AND d BETWEEN 110 AND 120
   AND a>b
   AND b>c
   AND d>e
   AND e+d BETWEEN a+b+10 AND c+130
 ORDER BY 1,2
 LIMIT 4

query IIIIIIIIIIIIIIIII nosort
SELECT (SELECT max(c)-max(d) FROM t1),
       a-b,
       b-c,
       a,
       a-e,
       c,
       a-c,
       (SELECT max(a) FROM t1),
       d,
       coalesce(a,b,c,d,e),
       b-d,
       b,
       c-d,
       (SELECT min(b) FROM t1),
       abs(a+b),
       a-d,
       c-e
  FROM t1
 WHERE c>d
 ORDER BY 15,12,1,3,4,14,9,10,7,11,6,2,13,17,16,5,8
 LIMIT 4

query II nosort
SELECT coalesce(a,b,c,d,e),
       b-e
  FROM t1
 ORDER BY 1,2
 LIMIT 4

query IIIIIIII nosort
SELECT a-c,
       b-d,
       b-e,
       c,
       e,
       d-e,
       (SELECT max(a) FROM t1),
       abs(a)
  FROM t1
 WHERE c BETWEEN b AND d
    OR b>c
    OR d>e
 ORDER BY 5,2,4,3,6,1,8,7
 LIMIT 4

query II nosort
SELECT a-e,
       (SELECT max(c)-max(d) FROM t1)
  FROM t1
 WHERE d BETWEEN 110 AND 120
    OR e+d BETWEEN a+b+10 AND c+130
    OR c>d
    OR d>e
    OR c BETWEEN b AND d
 ORDER BY 1,2
 LIMIT 4

query IIIII nosort
SELECT a-d,
       b-e,
       (SELECT min(b) FROM t1),
       b-d,
       (SELECT max(a) FROM t1)
  FROM t1
 WHERE c BETWEEN b AND d
   AND d>e
 ORDER BY 2,4,5,1,3
 LIMIT 4

query IIIIII nosort
SELECT a-c,
       abs(a+b),
       b-e,
       e,
       a-d,
       b
  FROM t1
 WHERE d>e
   AND c BETWEEN b AND d
   AND b>c
   AND d BETWEEN 110 AND 120
 ORDER BY 5,1,2,4,6,3
 LIMIT 4

query I nosort
SELECT abs(a+b)
  FROM t1
 WHERE d BETWEEN 110 AND 120
    OR c>d
    OR b>c
    OR a>b
    OR d>e
    OR e+d BETWEEN a+b+10 AND c+130
 ORDER BY 1
 LIMIT 4

query IIIIIIIIIIIIIII nosort
SELECT (SELECT min(b) FROM t1),
       e,
       b-d,
       c-d,
       abs(a+b),
       c-e,
       abs(a),
       d-e,
       (SELECT max(a) FROM t1),
       a-c,
       a-b,
       d,
       b-e,
       c,
       b-c
  FROM t1
 WHERE c BETWEEN b AND d
 ORDER BY 8,12,6,14,15,13,10,2,9,5,7,3,4,11,1
 LIMIT 4

query IIIIIIIIIIIIII nosort
SELECT abs(a),
       d-e,
       d,
       (SELECT min(b) FROM t1),
       a-d,
       b,
       (SELECT max(c)-max(d) FROM t1),
       c-d,
       a-c,
       b-c,
       c,
       a,
       b-d,
       a-b
  FROM t1
 WHERE a>b
    OR c>d
    OR d>e
 ORDER BY 13,14,5,10,11,1,9,6,2,12,8,3,7,4
 LIMIT 4

query IIIIIII nosort
SELECT e,
       b-e,
       c-d,
       d-e,
       d,
       c,
       (SELECT max(a) FROM t1)
  FROM t1
 WHERE d BETWEEN 110 AND 120
    OR c>d
    OR b>c
    OR d>e
 ORDER BY 3,2,7,5,4,6,1
 LIMIT 4

query IIII nosort
SELECT d,
       (SELECT max(c)-max(d) FROM t1),
       abs(a),
       d-e
  FROM t1
 WHERE e+d BETWEEN a+b+10 AND c+130
    OR a>b
    OR d>e
 ORDER BY 2,4,1,3
 LIMIT 4

query IIIII nosort
SELECT e,
       c-d,
       a-b,
       d,
       abs(a)
  FROM t1
 WHERE c BETWEEN b AND d
 ORDER BY 4,3,1,5,2
 LIMIT 4

query IIIIII nosort
SELECT b-d,
       b-c,
       c-e,
       d,
       b-e,
       a-d
  FROM t1
 WHERE c>d
   AND e+d BETWEEN a+b+10 AND c+130
   AND d>e
   AND a>b
   AND b>c
 ORDER BY 4,1,6,3,2,5
 LIMIT 4

query IIIIIIIIIIIIIIIIII nosort
SELECT coalesce(a,b,c,d,e),
       a-d,
       b-e,
       c,
       a-c,
       b,
       a,
       e,
       (SELECT max(a) FROM t1),
       c-e,
       d,
       (SELECT min(b) FROM t1),
       b-d,
       d-e,
       a-b,
       (SELECT max(c)-max(d) FROM t1),
       a-e,
       abs(a)
  FROM t1
 WHERE d BETWEEN 110 AND 120
 ORDER BY 15,10,3,6,1,11,2,8,16,9,17,13,4,14,7,5,18,12
 LIMIT 4

query IIIIIIIIIIIIIIII nosort
SELECT (SELECT max(a) FROM t1),
       a-c,
       (SELECT max(c)-max(d) FROM t1),
       (SELECT min(b) FROM t1),
       abs(a),
       b-d,
       a-b,
       d,
       a,
       e,
       b,
       b-e,
       c,
       c-d,
       a-d,
       a-e
  FROM t1
 WHERE e+d BETWEEN a+b+10 AND c+130
    OR b>c
    OR c>d
    OR d>e
    OR d BETWEEN 110 AND 120
 ORDER BY 12,16,2,6,3,14,13,9,5,15,11,1,10,4,7,8
 LIMIT 4

query IIIIII nosort
SELECT e,
       a-c,
       c-e,
       b-c,
       d,
       b-d
  FROM t1
 WHERE c>d
    OR d BETWEEN 110 AND 120
    OR a>b
    OR d>e
 ORDER BY 6,5,3,4,2,1
 LIMIT 4

query IIIIIIIIIII nosort
SELECT d,
       b-e,
       c,
       coalesce(a,b,c,d,e),
       e,
       (SELECT max(c)-max(d) FROM t1),
       (SELECT min(b) FROM t1),
       b,
       c-d,
       a-b,
       a-c
  FROM t1
 WHERE c>d
    OR e+d BETWEEN a+b+10 AND c+130
    OR d>e
    OR d BETWEEN 110 AND 120
    OR c BETWEEN b AND d
 ORDER BY 9,3,5,1,10,2,7,11,6,8,4
 LIMIT 4

query IIIIIIIIIIIIIIIIIII nosort
SELECT c-d,
       c,
       a-e,
       a-b,
       abs(a+b),
       (SELECT max(c)-max(d) FROM t1),
       a-d,
       b-c,
       (SELECT max(a) FROM t1),
       e,
       d-e,
       c-e,
       b,
       b-d,
       abs(a),
       d,
       b-e,
       coalesce(a,b,c,d,e),
       (SELECT min(b) FROM t1)
  FROM t1
 WHERE c BETWEEN b AND d
 ORDER BY 19,5,11,2,3,8,15,13,14,17,18,12,7,4,9,16,10,1,6
 LIMIT 4

query IIIIIIIIIIIIII nosort
SELECT b-e,
       a-d,
       e,
       abs(a+b),
       (SELECT max(c)-max(d) FROM t1),
       b-c,
       b,
       d,
       a,
       b-d,
       (SELECT min(b) FROM t1),
       a-c,
       c-e,
       d-e
  FROM t1
 WHERE b>c
    OR d BETWEEN 110 AND 120
    OR c BETWEEN b AND d
    OR e+d BETWEEN a+b+10 AND c+130
 ORDER BY 5,6,7,9,11,1,3,14,8,4,12,10,2,13
 LIMIT 4

query IIIIIIIIIIIIIIIIII nosort
SELECT a-c,
       b-c,
       (SELECT min(b) FROM t1),
       a-e,
       e,
       (SELECT max(c)-max(d) FROM t1),
       a,
       b,
       c,
       a-b,
       c-e,
       (SELECT max(a) FROM t1),
       b-d,
       c-d,
       d,
       abs(a),
       a-d,
       b-e
  FROM t1
 WHERE c BETWEEN b AND d
    OR d>e
    OR b>c
 ORDER BY 9,15,12,8,7,1,5,3,13,14,18,4,17,11,16,2,6,10
 LIMIT 4

query IIIIIIIIIIIII nosort
SELECT b,
       (SELECT max(a) FROM t1),
       d,
       abs(a+b),
       c,
       a-b,
       c-d,
       e,
       a-c,
       d-e,
       (SELECT max(c)-max(d) FROM t1),
       c-e,
       a-d
  FROM t1
 WHERE c>d
   AND a>b
 ORDER BY 5,9,13,11,3,10,2,4,7,6,1,8,12
 LIMIT 4

query IIIIIIII nosort
SELECT c-d,
       d,
       abs(a),
       b-c,
       d-e,
       a-c,
       c-e,
       c
  FROM t1
 WHERE b>c
   AND c BETWEEN b AND d
   AND e+d BETWEEN a+b+10 AND c+130
 ORDER BY 4,7,5,2,8,6,1,3
 LIMIT 4

query IIIIIIIIIIIIIIIII nosort
SELECT d,
       a-d,
       (SELECT min(b) FROM t1),
       abs(a+b),
       b-d,
       a-c,
       c,
       c-d,
       (SELECT max(a) FROM t1),
       a,
       b,
       (SELECT max(c)-max(d) FROM t1),
       a-e,
       c-e,
       b-e,
       coalesce(a,b,c,d,e),
       b-c
  FROM t1
 WHERE c BETWEEN b AND d
    OR d>e
    OR a>b
    OR d BETWEEN 110 AND 120
    OR b>c
 ORDER BY 15,16,12,5,14,3,7,9,10,17,2,13,4,1,8,6,11
 LIMIT 4

query IIIIIIIIIIIII nosort
SELECT c-e,
       b,
       (SELECT min(b) FROM t1),
       a-e,
       a-d,
       (SELECT max(a) FROM t1),
       c-d,
       abs(a+b),
       e,
       c,
       coalesce(a,b,c,d,e),
       abs(a),
       a
  FROM t1
 WHERE c>d
 ORDER BY 10,9,1,7,11,4,3,13,5,12,8,6,2
 LIMIT 4

query IIIIIIIIIIIIIII nosort
SELECT b-d,
       c-e,
       abs(a),
       (SELECT max(c)-max(d) FROM t1),
       e,
       a-e,
       (SELECT min(b) FROM t1),
       b-c,
       a,
       coalesce(a,b,c,d,e),
       c-d,
       (SELECT max(a) FROM t1),
       b,
       c,
       d
  FROM t1
 WHERE d BETWEEN 110 AND 120
   AND b>c
   AND d>e
 ORDER BY 4,12,14,15,7,8,6,9,13,3,1,2,5,10,11
 LIMIT 4

query IIIII nosort
SELECT b-e,
       a,
       a-d,
       c-e,
       d-e
  FROM t1
 WHERE d BETWEEN 110 AND 120
 ORDER BY 1,3,4,5,2
 LIMIT 4

query III nosort
SELECT b-e,
       a-b,
       c
  FROM t1
 WHERE c BETWEEN b AND d
   AND d>e
   AND d BETWEEN 110 AND 120
 ORDER BY 3,1,2
 LIMIT 4

query IIIIIIIIIIIIIIIIIIII nosort
SELECT coalesce(a,b,c,d,e),
       c-d,
       d,
       b-e,
       abs(a),
       c,
       (SELECT max(a) FROM t1),
       abs(a+b),
       b-d,
       b,
       a,
       e,
       b-c,
       a-e,
       (SELECT max(c)-max(d) FROM t1),
       a-c,
       c-e,
       d-e,
       a-d,
       a-b
  FROM t1
 WHERE d BETWEEN 110 AND 120
   AND c>d
 ORDER BY 9,18,12,13,4,17,20,11,19,8,2,7,5,10,14,6,1,15,3,16
 LIMIT 4

query II nosort
SELECT e,
       abs(a)
  FROM t1
 WHERE e+d BETWEEN a+b+10 AND c+130
   AND c BETWEEN b AND d
   AND a>b
   AND d>e
 ORDER BY 1,2
 LIMIT 4

query IIII nosort
SELECT b,
       (SELECT max(c)-max(d) FROM t1),
       coalesce(a,b,c,d,e),
       c-e
  FROM t1
 WHERE d BETWEEN 110 AND 120
 ORDER BY 2,4,1,3
 LIMIT 4

query IIIIIIIIIIIIIIIIII nosort
SELECT a,
       e,
       b,
       b-e,
       d-e,
       a-e,
       b-c,
       b-d,
       abs(a+b),
       c-e,
       abs(a),
       c,
       d,
       coalesce(a,b,c,d,e),
       (SELECT min(b) FROM t1),
       c-d,
       a-b,
       (SELECT max(c)-max(d) FROM t1)
  FROM t1
 WHERE b>c
   AND e+d BETWEEN a+b+10 AND c+130
   AND d>e
 ORDER BY 5,18,13,10,2,9,6,16,7,1,17,11,4,8,15,12,3,14
 LIMIT 4

query IIIIIIIIIIIIIIIII nosort
SELECT a-d,
       a-e,
       b-e,
       c,
       c-e,
       (SELECT max(a) FROM t1),
       (SELECT min(b) FROM t1),
       (SELECT max(c)-max(d) FROM t1),
       c-d,
       b-c,
       coalesce(a,b,c,d,e),
       d-e,
       b-d,
       d,
       a-c,
       a-b,
       e
  FROM t1
 WHERE b>c
    OR c>d
    OR c BETWEEN b AND d
    OR d>e
 ORDER BY 5,15,13,9,11,6,10,12,14,4,7,17,16,1,3,2,8
 LIMIT 4

query IIIIIIIIIIII nosort
SELECT c-e,
       b-e,
       d,
       d-e,
       (SELECT min(b) FROM t1),
       a-b,
       a-c,
       a,
       abs(a),
       e,
       c,
       b
  FROM t1
 WHERE b>c
    OR a>b
    OR d BETWEEN 110 AND 120
    OR e+d BETWEEN a+b+10 AND c+130
    OR d>e
 ORDER BY 2,4,12,6,1,9,5,8,11,7,10,3
 LIMIT 4

query IIIIIIIIIII nosort
SELECT c-e,
       e,
       abs(a+b),
       b-c,
       (SELECT max(a) FROM t1),
       c,
       (SELECT min(b) FROM t1),
       abs(a),
       c-d,
       coalesce(a,b,c,d,e),
       (SELECT max(c)-max(d) FROM t1)
  FROM t1
 WHERE e+d BETWEEN a+b+10 AND c+130
    OR a>b
    OR d>e
    OR c>d
    OR b>c
 ORDER BY 10,6,5,3,1,8,4,9,11,7,2
 LIMIT 4

query IIIIII nosort
SELECT a-c,
       abs(a),
       c,
       a-e,
       b-e,
       coalesce(a,b,c,d,e)
  FROM t1
 WHERE d>e
   AND e+d BETWEEN a+b+10 AND c+130
   AND c>d
   AND d BETWEEN 110 AND 120
   AND c BETWEEN b AND d
 ORDER BY 3,6,5,2,4,1
 LIMIT 4

query IIIIIIII nosort
SELECT abs(a+b),
       (SELECT min(b) FROM t1),
       a-d,
       a,
       (SELECT max(c)-max(d) FROM t1),
       c-d,
       coalesce(a,b,c,d,e),
       b
  FROM t1
 WHERE e+d BETWEEN a+b+10 AND c+130
   AND d BETWEEN 110 AND 120
   AND d>e
   AND a>b
   AND c BETWEEN b AND d
   AND b>c
 ORDER BY 5,1,6,7,4,2,3,8
 LIMIT 4

query IIIIIIIIIIIIIIIIIIII nosort
SELECT b,
       c,
       a,
       a-c,
       abs(a+b),
       b-e,
       abs(a),
       coalesce(a,b,c,d,e),
       a-d,
       b-d,
       a-b,
       c-e,
       d,
       (SELECT max(a) FROM t1),
       (SELECT max(c)-max(d) FROM t1),
       e,
       (SELECT min(b) FROM t1),
       c-d,
       d-e,
       a-e
  FROM t1
 WHERE a>b
 ORDER BY 3,7,15,8,4,12,18,6,13,10,16,20,2,17,1,11,5,19,14,9
 LIMIT 4

query IIIIIIIIIIIIIIIIIII nosort
SELECT d,
       b-c,
       a-c,
       (SELECT min(b) FROM t1),
       a-b,
       a-d,
       b-d,
       coalesce(a,b,c,d,e),
       abs(a+b),
       abs(a),
       d-e,
       b,
       c-e,
       e,
       c-d,
       a-e,
       (SELECT max(c)-max(d) FROM t1),
       b-e,
       c
  FROM t1
 WHERE d BETWEEN 110 AND 120
 ORDER BY 4,3,19,15,14,9,13,12,18,2,16,11,7,1,6,5,10,17,8
 LIMIT 4

query IIIIIIIIIIIIIIIIIIII nosort
SELECT (SELECT max(c)-max(d) FROM t1),
       c-d,
       d,
       a-b,
       c-e,
       b-d,
       (SELECT max(a) FROM t1),
       a,
       b-c,
       c,
       (SELECT min(b) FROM t1),
       b,
       coalesce(a,b,c,d,e),
       a-c,
       d-e,
       abs(a+b),
       b-e,
       e,
       abs(a),
       a-e
  FROM t1
 WHERE a>b
   AND d>e
   AND d BETWEEN 110 AND 120
 ORDER BY 3,9,1,16,15,7,13,6,19,11,5,4,18,12,10,14,20,17,2,8
 LIMIT 4

query I nosort
SELECT e
  FROM t1
 WHERE d>e
   AND c>d
 ORDER BY 1
 LIMIT 4

query IIIIIIIIIIIIIIII nosort
SELECT b,
       d-e,
       (SELECT max(a) FROM t1),
       b-d,
       (SELECT min(b) FROM t1),
       abs(a),
       coalesce(a,b,c,d,e),
       b-c,
       a-e,
       a,
       a-d,
       c,
       c-e,
       (SELECT max(c)-max(d) FROM t1),
       a-b,
       a-c
  FROM t1
 WHERE b>c
   AND e+d BETWEEN a+b+10 AND c+130
   AND d>e
 ORDER BY 5,13,10,7,16,14,3,1,2,4,8,11,9,6,12,15
 LIMIT 4

query II nosort
SELECT e,
       b-d
  FROM t1
 WHERE d>e
   AND b>c
   AND c BETWEEN b AND d
   AND a>b
 ORDER BY 1,2
 LIMIT 4

query IIIIIII nosort
SELECT e,
       b-d,
       a-d,
       (SELECT min(b) FROM t1),
       c-e,
       abs(a),
       abs(a+b)
  FROM t1
 WHERE e+d BETWEEN a+b+10 AND c+130
 ORDER BY 1,3,2,7,6,4,5
 LIMIT 4

query IIIIIIIIIIIIIIII nosort
SELECT a-b,
       b-c,
       a,
       b-d,
       c-e,
       b,
       (SELECT min(b) FROM t1),
       b-e,
       abs(a),
       a-d,
       (SELECT max(a) FROM t1),
       abs(a+b),
       a-c,
       a-e,
       (SELECT max(c)-max(d) FROM t1),
       coalesce(a,b,c,d,e)
  FROM t1
 ORDER BY 11,9,6,14,7,12,2,8,3,1,15,4,13,10,5,16
 LIMIT 4

query IIIIIIIIIIIIIIIIIIII nosort
SELECT (SELECT max(a) FROM t1),
       b-e,
       d,
       e,
       a-e,
       a-d,
       abs(a+b),
       a-c,
       (SELECT min(b) FROM t1),
       (SELECT max(c)-max(d) FROM t1),
       c,
       d-e,
       b-d,
       c-e,
       coalesce(a,b,c,d,e),
       b,
       a-b,
       a,
       c-d,
       abs(a)
  FROM t1
 WHERE d BETWEEN 110 AND 120
    OR c>d
    OR a>b
    OR c BETWEEN b AND d
    OR b>c
    OR d>e
 ORDER BY 15,5,19,7,20,17,4,18,14,16,8,10,11,6,1,13,2,9,3,12
 LIMIT 4

query IIIIIIIIIIIIIIIII nosort
SELECT (SELECT max(a) FROM t1),
       (SELECT min(b) FROM t1),
       d,
       a-e,
       c-d,
       c,
       a-d,
       b-d,
       b-c,
       d-e,
       e,
       c-e,
       a-b,
       (SELECT max(c)-max(d) FROM t1),
       b-e,
       a,
       a-c
  FROM t1
 WHERE c BETWEEN b AND d
 ORDER BY 12,2,11,5,14,9,10,17,16,7,4,6,1,13,15,3,8
 LIMIT 4

query IIIII nosort
SELECT a-c,
       b-c,
       c,
       c-d,
       (SELECT min(b) FROM t1)
  FROM t1
 ORDER BY 2,5,4,1,3
 LIMIT 4

query IIII nosort
SELECT c-e,
       a-d,
       abs(a+b),
       a-e
  FROM t1
 WHERE b>c
    OR a>b
    OR e+d BETWEEN a+b+10 AND c+130
    OR c BETWEEN b AND d
    OR d>e
 ORDER BY 1,2,3,4
 LIMIT 4

query IIIIIIIIIIIIIIIII nosort
SELECT c-e,
       a-b,
       a-e,
       abs(a),
       (SELECT max(c)-max(d) FROM t1),
       coalesce(a,b,c,d,e),
       c,
       e,
       b-e,
       a-c,
       d,
       a,
       abs(a+b),
       d-e,
       b-c,
       b-d,
       (SELECT max(a) FROM t1)
  FROM t1
 WHERE d BETWEEN 110 AND 120
   AND e+d BETWEEN a+b+10 AND c+130
 ORDER BY 17,8,1,16,10,11,15,4,9,6,7,5,12,13,2,14,3
 LIMIT 4

query IIIIIIIIIIIIIIIIIIII nosort
SELECT b-e,
       e,
       d,
       (SELECT max(a) FROM t1),
       d-e,
       c-d,
       b-c,
       a-b,
       c-e,
       a-e,
       (SELECT min(b) FROM t1),
       coalesce(a,b,c,d,e),
       abs(a+b),
       a-d,
       abs(a),
       b-d,
       b,
       a-c,
       (SELECT max(c)-max(d) FROM t1),
       c
  FROM t1
 WHERE a>b
   AND d BETWEEN 110 AND 120
   AND c BETWEEN b AND d
 ORDER BY 5,3,12,7,1,17,11,18,20,13,6,2,16,14,15,9,8,10,19,4
 LIMIT 4

query III nosort
SELECT a-e,
       c-e,
       abs(a+b)
  FROM t1
 WHERE b>c
 ORDER BY 2,3,1
 LIMIT 4

query IIIIIIIIIIIIIIIIIII nosort
SELECT b,
       abs(a),
       (SELECT min(b) FROM t1),
       (SELECT max(a) FROM t1),
       a-c,
       b-e,
       c-e,
       abs(a+b),
       c,
       c-d,
       a-e,
       a-b,
       (SELECT max(c)-max(d) FROM t1),
       b-d,
       d-e,
       a-d,
       e,
       b-c,
       coalesce(a,b,c,d,e)
  FROM t1
 WHERE c>d
    OR e+d BETWEEN a+b+10 AND c+130
    OR b>c
 ORDER BY 5,1,15,7,13,18,3,14,6,17,2,12,19,4,16,11,9,10,8
 LIMIT 4

query IIIIIII nosort
SELECT abs(a+b),
       a-c,
       coalesce(a,b,c,d,e),
       b-c,
       (SELECT min(b) FROM t1),
       c-d,
       d-e
  FROM t1
 WHERE d BETWEEN 110 AND 120
   AND c BETWEEN b AND d
   AND b>c
   AND c>d
 ORDER BY 1,7,6,5,2,4,3
 LIMIT 4

query IIIIIIIIIIIII nosort
SELECT d-e,
       (SELECT max(a) FROM t1),
       b-e,
       a-b,
       e,
       abs(a+b),
       d,
       b,
       a-e,
       c-e,
       (SELECT min(b) FROM t1),
       c,
       abs(a)
  FROM t1
 WHERE b>c
    OR c>d
 ORDER BY 13,1,3,10,7,12,11,2,5,8,6,4,9
 LIMIT 4

query IIIIIIIIIIIIII nosort
SELECT b-c,
       d-e,
       e,
       b-e,
       (SELECT max(a) FROM t1),
       coalesce(a,b,c,d,e),
       a,
       a-d,
       b,
       d,
       b-d,
       c-e,
       a-c,
       c
  FROM t1
 ORDER BY 1,6,14,3,2,10,12,11,4,9,5,8,13,7
 LIMIT 4

query IIIIIIIIIII nosort
SELECT a-d,
       d,
       b-e,
       c,
       (SELECT max(c)-max(d) FROM t1),
       (SELECT min(b) FROM t1),
       a,
       b-d,
       d-e,
       b,
       a-e
  FROM t1
 ORDER BY 11,7,9,3,10,8,6,4,1,5,2
 LIMIT 4

query IIIIIIIIIIIIIIII nosort
SELECT a-c,
       a-e,
       e,
       b,
       c-e,
       d-e,
       coalesce(a,b,c,d,e),
       c-d,
       abs(a),
       c,
       b-d,
       (SELECT min(b) FROM t1),
       (SELECT max(c)-max(d) FROM t1),
       a-d,
       b-e,
       a-b
  FROM t1
 WHERE d BETWEEN 110 AND 120
 ORDER BY 9,16,8,6,1,3,15,13,5,12,10,2,4,14,11,7
 LIMIT 4

query IIIIIIIIIIII nosort
SELECT a-c,
       b,
       (SELECT max(c)-max(d) FROM t1),
       abs(a+b),
       b-d,
       a,
       a-d,
       c-d,
       d-e,
       (SELECT min(b) FROM t1),
       a-e,
       (SELECT max(a) FROM t1)
  FROM t1
 WHERE d>e
 ORDER BY 11,3,8,6,2,9,12,7,10,1,5,4
 LIMIT 4

query III nosort
SELECT d-e,
       e,
       abs(a)
  FROM t1
 ORDER BY 2,1,3
 LIMIT 4

query IIIIIII nosort
SELECT e,
       abs(a+b),
       a-c,
       a,
       b-e,
       a-d,
       (SELECT max(c)-max(d) FROM t1)
  FROM t1
 ORDER BY 5,2,7,6,3,1,4
 LIMIT 4

query IIIIIII nosort
SELECT a-d,
       abs(a),
       b,
       b-e,
       c-e,
       coalesce(a,b,c,d,e),
       b-c
  FROM t1
 WHERE c BETWEEN b AND d
    OR d BETWEEN 110 AND 120
    OR d>e
    OR c>d
    OR a>b
    OR e+d BETWEEN a+b+10 AND c+130
 ORDER BY 5,7,2,4,6,1,3
 LIMIT 4

query IIIIIIIIIIIIII nosort
SELECT a-e,
       a,
       d-e,
       c-d,
       b-d,
       abs(a),
       c-e,
       a-b,
       (SELECT max(a) FROM t1),
       coalesce(a,b,c,d,e),
       b,
       b-e,
       (SELECT max(c)-max(d) FROM t1),
       abs(a+b)
  FROM t1
 WHERE d>e
 ORDER BY 2,3,12,4,7,1,5,10,11,9,6,8,13,14
 LIMIT 4

query IIII nosort
SELECT c-d,
       coalesce(a,b,c,d,e),
       (SELECT max(c)-max(d) FROM t1),
       c
  FROM t1
 WHERE d>e
   AND a>b
   AND c BETWEEN b AND d
   AND c>d
   AND d BETWEEN 110 AND 120
   AND e+d BETWEEN a+b+10 AND c+130
 ORDER BY 1,3,4,2
 LIMIT 4

query IIIIIIIIIIIIIIIIIII nosort
SELECT a-c,
       abs(a+b),
       d-e,
       (SELECT min(b) FROM t1),
       e,
       a-d,
       b-c,
       a-e,
       a-b,
       b,
       (SELECT max(a) FROM t1),
       abs(a),
       d,
       b-e,
       c-d,
       coalesce(a,b,c,d,e),
       b-d,
       c-e,
       (SELECT max(c)-max(d) FROM t1)
  FROM t1
 WHERE a>b
    OR c>d
    OR d>e
 ORDER BY 14,6,13,17,9,10,1,15,2,16,4,3,11,8,7,18,12,19,5
 LIMIT 4

query IIIIIIIIIIIII nosort
SELECT b-c,
       b-d,
       b-e,
       c,
       abs(a),
       coalesce(a,b,c,d,e),
       a-e,
       a,
       d,
       a-c,
       (SELECT max(a) FROM t1),
       a-b,
       c-d
  FROM t1
 ORDER BY 5,2,9,4,10,7,1,12,6,8,3,13,11
 LIMIT 4

